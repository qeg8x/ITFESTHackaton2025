# 🎓 Цифровой университет

> MVP платформы для автоматизированного сбора и отображения информации о вузах

[![Deno](https://img.shields.io/badge/Deno-1.38+-blue?logo=deno)](https://deno.land)
[![Fresh](https://img.shields.io/badge/Fresh-1.6-green)](https://fresh.deno.dev)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15-blue?logo=postgresql)](https://postgresql.org)
[![License](https://img.shields.io/badge/License-MIT-yellow)](LICENSE)

---

## 📋 Содержание

- [О проекте](#о-проекте)
- [Технологический стек](#технологический-стек)
- [Быстрый старт](#быстрый-старт)
- [Структура проекта](#структура-проекта)
- [Основные компоненты](#основные-компоненты)
- [API](#api)
- [Дополнительная документация](#дополнительная-документация)

---

## 🎯 О проекте

**Цифровой университет** — это MVP платформы, которая:

- 🔍 **Собирает данные** с сайтов университетов автоматически
- 🤖 **Использует AI** (Ollama LLM) для парсинга и структурирования информации
- 📊 **Отображает профили** вузов с программами, стоимостью и контактами
- 🔄 **Обновляется автоматически** через фоновый воркер

### Ключевые возможности

| Функция | Описание |
|---------|----------|
| Поиск университетов | Поиск по названию, стране, городу |
| Профили вузов | Полная информация: программы, стоимость, требования |
| AI парсинг | Автоматическое извлечение данных с сайтов |
| Детекция изменений | Отслеживание обновлений через хэширование |
| REST API | Полноценный API для интеграций |

---

## 🛠 Технологический стек

### Backend
- **Runtime**: [Deno](https://deno.land) 1.38+
- **Framework**: [Fresh](https://fresh.deno.dev) 1.6
- **Database**: [PostgreSQL](https://postgresql.org) 15+
- **AI**: [Ollama](https://ollama.ai) (локальный LLM)

### Frontend
- **UI Library**: [Preact](https://preactjs.com)
- **Styling**: [Tailwind CSS](https://tailwindcss.com) (via twind)
- **State**: [@preact/signals](https://preactjs.com/guide/v10/signals)

### Infrastructure
- **Container**: Docker & Docker Compose
- **CI/CD**: GitHub Actions

---

## 🚀 Быстрый старт

### Требования

- Deno >= 1.38
- PostgreSQL >= 14
- Ollama (опционально)

### Установка

```bash
# 1. Клонировать репозиторий
git clone https://github.com/your-repo/digital-university.git
cd digital-university

# 2. Настроить окружение
cp .env.example .env
# Отредактировать .env

# 3. Создать базу данных
createdb digital_university

# 4. Запустить
deno task dev
```

### Docker (рекомендуется)

```bash
# Запустить все сервисы
docker-compose up -d

# Открыть в браузере
open http://localhost:8000
```

---

## 📁 Структура проекта

```
digital-university/
├── routes/                    # Fresh роуты (страницы и API)
│   ├── api/                   # REST API эндпоинты
│   │   ├── universities/      # CRUD университетов
│   │   ├── admin/             # Админ эндпоинты
│   │   ├── parser.ts          # Парсинг API
│   │   ├── filters.ts         # Фильтры
│   │   └── debug.ts           # Health & debug
│   ├── universities/          # Страницы университетов
│   ├── _app.tsx               # Root layout
│   ├── _404.tsx               # 404 страница
│   ├── _500.tsx               # 500 страница
│   └── index.tsx              # Главная страница
│
├── islands/                   # Интерактивные компоненты (client-side)
│   ├── HomePage.tsx           # Главная страница
│   ├── UniversitySelector.tsx # Поиск университетов
│   └── UniversityProfile.tsx  # Профиль университета
│
├── components/                # Статические компоненты (server-side)
│   ├── Button.tsx             # Кнопки
│   ├── Card.tsx               # Карточки
│   ├── Badge.tsx              # Бейджи
│   ├── Loading.tsx            # Загрузка
│   └── ProgramsList.tsx       # Список программ
│
├── src/                       # Бизнес-логика
│   ├── config/                # Конфигурация
│   │   ├── database.ts        # PostgreSQL подключение
│   │   └── env.ts             # Переменные окружения
│   ├── db/                    # Работа с БД
│   │   ├── migrations.ts      # Миграции
│   │   └── seed.ts            # Тестовые данные
│   ├── services/              # Сервисы
│   │   ├── universities.service.ts  # CRUD университетов
│   │   └── parser.service.ts  # Парсинг сайтов
│   ├── workers/               # Фоновые задачи
│   │   └── update.worker.ts   # Воркер обновления
│   ├── utils/                 # Утилиты
│   │   ├── logger.ts          # Логирование
│   │   ├── ollama.client.ts   # Ollama API клиент
│   │   └── markdown.converter.ts  # HTML → Markdown
│   └── types/                 # TypeScript типы
│       ├── university.ts      # Типы университета
│       └── database.ts        # Типы БД
│
├── sql/                       # SQL миграции
│   └── 001_initial_schema.sql
│
├── scripts/                   # Скрипты
│   ├── setup-db.ts            # Инициализация БД
│   └── test.sh                # Тестирование API
│
├── static/                    # Статические файлы
├── docs/                      # Документация
│
├── main.ts                    # Точка входа (production)
├── dev.ts                     # Точка входа (development)
├── fresh.config.ts            # Конфигурация Fresh
├── twind.config.ts            # Конфигурация Tailwind
├── deno.json                  # Deno конфигурация
├── Dockerfile                 # Docker образ
└── docker-compose.yml         # Docker Compose
```

---

## 🧩 Основные компоненты

### 1. API Layer (`routes/api/`)

REST API для работы с данными:

```
GET  /api/universities          # Список университетов
GET  /api/universities/:id      # Университет по ID
GET  /api/universities/:id/profile  # Профиль
POST /api/universities/:id/profile  # Обновить профиль
GET  /api/filters               # Фильтры (страны, города)
GET  /api/parser                # Статус парсера
POST /api/parser                # Запуск парсинга
GET  /api/debug?action=health   # Health check
```

### 2. Services (`src/services/`)

- **universities.service.ts** — CRUD операции с университетами
- **parser.service.ts** — парсинг сайтов и AI обработка

### 3. Workers (`src/workers/`)

- **update.worker.ts** — фоновое обновление данных каждые 60 сек (dev) или 24 часа (prod)

### 4. Islands (`islands/`)

Интерактивные Preact компоненты:
- **HomePage** — главная с поиском и профилем
- **UniversitySelector** — автокомплит поиска
- **UniversityProfile** — отображение профиля

---

## 📡 API

Подробная документация: [API.md](./API.md)

### Примеры

```bash
# Список университетов
curl http://localhost:8000/api/universities

# Конкретный университет
curl http://localhost:8000/api/universities/UUID

# Health check
curl "http://localhost:8000/api/debug?action=health"
```

---

## 📚 Дополнительная документация

| Документ | Описание |
|----------|----------|
| [ARCHITECTURE.md](./ARCHITECTURE.md) | Архитектура системы |
| [DEVELOPMENT.md](./DEVELOPMENT.md) | Настройка разработки |
| [DEPLOYMENT.md](./DEPLOYMENT.md) | Развёртывание |
| [API.md](./API.md) | API документация |
| [TESTING.md](./TESTING.md) | Тестирование |
| [CHANGELOG.md](../CHANGELOG.md) | История версий |

---

## 📄 Лицензия

MIT License © 2024 Digital University Team

---

## 🤝 Контрибуция

1. Fork репозитория
2. Создать feature branch (`git checkout -b feature/amazing`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing`)
5. Открыть Pull Request
