# 🏗 Архитектура системы

## Обзор

Цифровой университет построен как **Fresh монолит** — единое приложение, которое включает:
- SSR (Server-Side Rendering) для SEO
- Islands для интерактивности
- REST API для данных
- Фоновый воркер для обновлений

---

## 📊 Диаграмма архитектуры

```
┌─────────────────────────────────────────────────────────────────┐
│                         КЛИЕНТ (Браузер)                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐  │
│  │  Главная    │  │   Поиск     │  │   Профиль университета  │  │
│  │  страница   │  │  (Island)   │  │       (Island)          │  │
│  └─────────────┘  └─────────────┘  └─────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼ HTTP
┌─────────────────────────────────────────────────────────────────┐
│                      FRESH СЕРВЕР (Deno)                        │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    Routes Layer                           │   │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────────────┐  │   │
│  │  │ /          │  │ /api/*     │  │ /universities/*    │  │   │
│  │  │ (SSR)      │  │ (REST API) │  │ (SSR + Islands)    │  │   │
│  │  └────────────┘  └────────────┘  └────────────────────┘  │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              │                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                   Services Layer                          │   │
│  │  ┌──────────────────────┐  ┌──────────────────────────┐  │   │
│  │  │ universities.service │  │    parser.service        │  │   │
│  │  │  - getAllUniversities│  │  - fetchAndHashWebsite   │  │   │
│  │  │  - getUniversityById │  │  - htmlToMarkdown        │  │   │
│  │  │  - updateProfile     │  │  - markdownToProfile     │  │   │
│  │  └──────────────────────┘  └──────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              │                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                   Workers Layer                           │   │
│  │  ┌─────────────────────────────────────────────────────┐ │   │
│  │  │              update.worker                           │ │   │
│  │  │  - Запуск каждые 60с (dev) / 24ч (prod)             │ │   │
│  │  │  - Проверка хэшей сайтов                            │ │   │
│  │  │  - Обновление через AI парсер                       │ │   │
│  │  └─────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
              ┌───────────────┼───────────────┐
              ▼               ▼               ▼
┌──────────────────┐ ┌──────────────┐ ┌──────────────────┐
│   PostgreSQL     │ │    Ollama    │ │  External Sites  │
│                  │ │    (LLM)     │ │  (Universities)  │
│  - universities  │ │              │ │                  │
│  - profiles      │ │  llama3      │ │  nu.edu.kz       │
│  - sources       │ │  mistral     │ │  msu.ru          │
│  - update_logs   │ │              │ │  ...             │
└──────────────────┘ └──────────────┘ └──────────────────┘
```

---

## 🔄 Поток данных

### 1. Отображение списка университетов

```
Browser → GET /api/universities
       → universities.service.getAllUniversities()
       → PostgreSQL query
       → JSON response
       → Preact render
```

### 2. Отображение профиля

```
Browser → GET /universities/:id (SSR)
       → handler → getUniversityWithProfile()
       → PostgreSQL query (universities + profiles)
       → Server-side render HTML
       → Hydration (Island активация)
```

### 3. Фоновое обновление

```
setInterval (60s / 24h)
  │
  ▼
update.worker → getSourcesForUpdate()
             → for each source:
                 │
                 ▼
               fetchAndHashWebsite(url)
                 │
                 ├─ hash не изменился → skip
                 │
                 └─ hash изменился:
                     │
                     ▼
                   htmlToMarkdown(html)
                     │
                     ▼
                   callOllama(markdown) → JSON profile
                     │
                     ▼
                   updateUniversityProfile()
                     │
                     ▼
                   logUpdate()
```

### 4. Ручной парсинг (Admin)

```
Admin → POST /api/parser { action: 'preview', url }
     → fetchAndHashWebsite(url)
     → htmlToMarkdown(html)
     → markdownToUniversityProfile(md) via Ollama
     → Return preview (не сохраняет)
```

---

## 📦 Описание модулей

### Routes (`routes/`)

| Файл | Описание |
|------|----------|
| `index.tsx` | Главная страница с поиском |
| `universities/index.tsx` | Каталог университетов |
| `universities/[id].tsx` | Страница профиля (SSR) |
| `api/universities/` | REST API университетов |
| `api/parser.ts` | API парсера |
| `api/debug.ts` | Health & debug |
| `_app.tsx` | Root layout (head, styles) |
| `_404.tsx`, `_500.tsx` | Error pages |

### Services (`src/services/`)

| Файл | Функции |
|------|---------|
| `universities.service.ts` | CRUD операции, пагинация, поиск |
| `parser.service.ts` | Fetch → Hash → Markdown → AI → JSON |

### Config (`src/config/`)

| Файл | Описание |
|------|----------|
| `database.ts` | PostgreSQL pool, query, transaction |
| `env.ts` | Загрузка и валидация .env |

### Workers (`src/workers/`)

| Файл | Описание |
|------|----------|
| `update.worker.ts` | Фоновое обновление с параллелизмом (max 3) |

### Utils (`src/utils/`)

| Файл | Описание |
|------|----------|
| `logger.ts` | Цветной вывод, уровни логов |
| `ollama.client.ts` | HTTP клиент для Ollama API |
| `markdown.converter.ts` | HTML → Markdown, SHA-256 хэш |

---

## 🏛 База данных

### Схема

```sql
universities          -- Базовая информация
├── id (UUID, PK)
├── name, name_en
├── country, city
├── website_url
└── is_active

university_profiles   -- JSONB профили (версионированные)
├── id (UUID, PK)
├── university_id (FK)
├── profile_json (JSONB)
├── language
└── version

university_sources    -- Источники для парсинга
├── id (UUID, PK)
├── university_id (FK)
├── url
├── source_type
├── current_hash      -- SHA-256 для детекции изменений
└── last_checked_at

update_logs           -- История обновлений
├── id (UUID, PK)
├── source_id (FK)
├── status (success/failed/skipped)
├── changes_detected
└── processing_time_ms
```

---

## ➕ Как добавить новый университет

### Способ 1: Через seed

1. Открыть `src/db/seed.ts`
2. Добавить в массив `SEED_UNIVERSITIES`:

```typescript
{
  name: 'Новый Университет',
  name_en: 'New University',
  country: 'Казахстан',
  city: 'Астана',
  website_url: 'https://new-uni.edu.kz',
  description: '...',
  programs: [...],
}
```

3. Запустить seed:

```bash
deno task db:seed
```

### Способ 2: Через API

```bash
# 1. Добавить университет (нужна миграция или SQL)
psql -d digital_university -c "
  INSERT INTO universities (name, country, city, website_url)
  VALUES ('Новый', 'Казахстан', 'Астана', 'https://...')
  RETURNING id;
"

# 2. Добавить источник
psql -d digital_university -c "
  INSERT INTO university_sources (university_id, url, source_type)
  VALUES ('UUID', 'https://...', 'website');
"

# 3. Запустить парсинг
curl -X POST "http://localhost:8000/api/admin/update-now?university_id=UUID" \
  -H "X-Admin-Key: dev-admin-key"
```

### Способ 3: Полный автомат (будущее)

В будущих версиях планируется:
- Admin UI для добавления университетов
- Автоматический поиск и добавление по URL
- Импорт из CSV/JSON
